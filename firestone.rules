rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /carritos/{userId} {
      // Solo el usuario dueño puede leer/escribir su carrito
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
